# C 语言速成（有基础版）｜15 章笔记直达嵌入式

## 第1章 C语言快速认知

**一、C 与其他语言的核心差异**

1. **编译型语言**：源码直接编译成机器码运行，无虚拟机、无解释器，执行效率极高
2. **手动内存管理**：无 GC，申请的内存自己释放；越界、泄漏都得自己兜底
3. **可直接操作内存**：指针能读写任意内存地址——这是 C 能写操作系统、驱动、单片机程序的根基
4. **语法极简**：仅 32 个关键字、9 种控制语句；标准库很薄，很多功能自己实现

**二、C 程序固定结构**

```c
#include <stdio.h>   // 预处理：把头文件内容"复制"进来（≈ import）

int main(void)        // 程序唯一入口（≈ Java 的 main / Python 的 __main__）
{
    printf("Hello World!\n");
    return 0;         // 返回0表示正常结束
}
```

**三、必守规则**

- 语句以英文半角分号 `;` 结尾；所有符号必须英文半角
- `//` 单行注释，`/* */` 多行注释
- C 是"信任程序员"的语言：不检查数组越界、不检查空指针（和`Rust`是两个极端）——**编译能过 ≠ 运行没错**

**四、编译运行 4 步【要懂】**

预处理（展开 `#include`/`#define`）→ 编译（生成目标文件 .o）→ 链接（合并库，生成可执行文件）→ 运行

嵌入式开发用的是**交叉编译**：在 PC 上编译出能在 MCU（如 STM32）上运行的固件，原理一样。

**练手环境速览**：以 Windows 为例，VS Code + C/C++ 插件 + GCC（Windows 装 MinGW）就够；真正烧进单片机，再用 STM32CubeIDE 或 VS Code + PlatformIO。

---

## 第2章 数据类型

**一、C 语言数据类型体系**

```text
数据类型
├─ 基本类型
│  ├─ 整型：char / short / int / long（可加 signed/unsigned）
│  ├─ 浮点型：float、double
│  └─ 字符型：char（本质是1字节整数）
├─ 构造类型：数组、结构体struct、共用体union、枚举enum
├─ 指针类型（C语言灵魂）
└─ 空类型：void
```

**二、有基础必看：与其他语言的 5 个关键区别**

1. **int 长度不固定**：随平台变化（16 位机 2 字节，32/64 位机 4 字节），不像 Java 定死 4 字节
2. **对长度敏感就认准定长类型**：引入 `<stdint.h>`，用 `uint8_t`、`uint16_t`、`uint32_t`——寄存器是几位就用几位，嵌入式代码里全是它们
3. **char 就是 1 字节整数**，可以直接参与数值运算
4. **C89 没有 bool**：非 0 即真、0 为假；C99 起可用 `<stdbool.h>` 的 `bool/true/false`
5. **浮点精度有限**：float 约 7 位有效数字；很多低端 MCU 没有 FPU，浮点运算靠软件模拟非常慢——能用整数就别用浮点

**三、常量与变量**

- 常量：普通常量 `10`、`3.14`、`'a'`、`"hello"`；符号常量 `#define PI 3.14159`（预处理直接替换，习惯全大写）
- 变量铁则：**必须先定义，后使用**，例：`int age = 18;`
- 命名规则：字母/数字/下划线组成，首字符不能是数字；不能用关键字；区分大小写

**四、32 个关键字分类速记**

| 分类     | 关键字                                                                                      |
| -------- | ------------------------------------------------------------------------------------------- |
| 数据类型 | char、short、int、long、float、double、signed、unsigned、void、enum、struct、union、typedef |
| 流程控制 | if、else、switch、case、default、for、while、do、break、continue、goto、return              |
| 存储类别 | auto、static、register、extern                                                              |
| 其他     | const、sizeof、volatile                                                                     |

## 第3章 运算符与表达式

**一、优先级必背口诀：非 > 算 > 关 > 与 > 或 > 赋**

**二、核心运算符速览**

| 运算符类型 | 核心规则                                                                                                                   |
| ---------- | -------------------------------------------------------------------------------------------------------------------------- |
| 算术运算符 | `+` `-` `*` `/` `%`：整数除法舍去小数（`7/2=3`）；`%` 取余只能用于整数；`++/--` 前置先变后用，后置先用后变 |
| 关系运算符 | `==` `!=` `>` `<` `>=` `<=`：结果只有真(1)/假(0)；**`==` 别写成 `=`**                                |
| 逻辑运算符 | `!` > `&&` > `                                                                                                         |
| 赋值运算符 | `=` `+=` `-=` 等，自右向左结合，优先级极低                                                                           |

**三、类型转换**

1. 自动转换：低精度→高精度，`char/short → int → long → float → double`
2. 强制转换：`(类型名)表达式`，例：`(int)3.14` 结果为 3，只生成临时值，不改变原变量
3. 常见坑：`int a=7, b=2; float c = a/b;` 结果还是 3——要先转再除：`float c = (float)a / b;`

**四、位运算【很多语言里用不到，嵌入式里天天用】**

| 运算符 | 名称     | 说明                |
| ------ | -------- | ------------------- |
| `&`  | 按位与   | 两位都为 1 才得 1   |
| `      | `        | 按位或              |
| `^`  | 按位异或 | 不同得 1，相同得 0  |
| `~`  | 按位取反 | 0 变 1，1 变 0      |
| `<<` | 左移     | 左移 n 位 ≈ 乘 2ⁿ |
| `>>` | 右移     | 右移 n 位 ≈ 除 2ⁿ |

**五、四大经典操作【背到肌肉记忆】**

设 `reg` 为一个变量，`n` 为要操作的位：

| 操作             | 写法                    | 效果                    |
| ---------------- | ----------------------- | ----------------------- |
| 置位（某位置1）  | `reg                    | = (1 << n);`            |
| 清位（某位置0）  | `reg &= ~(1 << n);`   | 第 n 位变 0，其他位不变 |
| 翻转（某位取反） | `reg ^= (1 << n);`    | 第 n 位 0↔1            |
| 判断某位         | `if (reg & (1 << n))` | 为真表示第 n 位是 1     |

**口诀：置位用或，清位用与非，翻转用异或，判断用与。**

为什么重要：**寄存器的每一位都控制一个硬件功能，读写寄存器 = 位运算**。权限标志、协议拆字节也全靠它。

```c
uint32_t reg = 0x00;
reg |= (1 << 3);        // 第3位置1  → 0x08
reg &= ~(1 << 3);       // 第3位清0  → 0x00
reg ^= (1 << 0);        // 第0位翻转 → 0x01
if (reg & (1 << 0)) { } // 判断第0位是否为1
```

---

## 第4章 顺序结构与输入输出

**一、C 语句 5 种分类【速览】**

1. 控制语句：if、for、while 等 9 种
2. 函数调用语句：`printf("hello");`
3. 表达式语句：表达式+分号，`a = 3 + 5;`
4. 空语句：仅一个 `;`，用于占位
5. 复合语句：`{}` 包裹的多条语句，整体视为一个语句

**二、printf 必背格式符**

| 格式符            | 功能                                                 |
| ----------------- | ---------------------------------------------------- |
| `%d`            | 十进制整数                                           |
| `%f`            | 小数（默认6位）；`%.2f` 保留2位                    |
| `%c`            | 单个字符                                             |
| `%s`            | 字符串                                               |
| `%x` / `%02X` | 十六进制（打寄存器值、日志高频；02 表示不足2位补零） |
| `%u`            | 无符号整数                                           |
| `%%`            | 输出百分号本身                                       |

**三、scanf 要点【避坑】**

- 变量名前必须加取地址符 `&`：`scanf("%d", &a);`
- 格式符必须和变量类型一一对应
- `%c` 读取时，空格、回车都算有效字符

**四、上手练一发：输入半径，求圆的周长和面积**

```c
#include <stdio.h>
#define PI 3.14159

int main(void)
{
    float r;
    printf("请输入圆的半径：");
    scanf("%f", &r);

    printf("周长：%.2f\n", 2 * PI * r);
    printf("面积：%.2f\n", PI * r * r);
    return 0;
}
```

> 冷知识：单片机没有屏幕键盘，`printf` 一般重定向到串口（UART），调试信息在电脑串口助手里看——串口就是你的显示器。

---

## 第5章 选择结构

> 和其他语言几乎一样，这章快速过，只划重点。

**一、核心逻辑**：表达式**非 0 为真，0 为假**

**二、if 三种标准形式**

```c
if (条件) { }                               // 单分支
if (条件) { } else { }                      // 双分支
if (条件1) { } else if (条件2) { } else { } // 多分支
```

- 嵌套规则：`else` 永远与**离它最近、未配对的 if** 配对
- 三目运算符：`max = a > b ? a : b;`

**三、switch 多分支【重点提醒】**

```c
switch (整型/字符型表达式)
{
    case 常量1: 语句块1; break;
    case 常量2: 语句块2; break;
    default: 语句块; break;
}
```

- **不加 break 会发生 case 穿透**（继续往下执行所有 case）——Python 的 match 不会穿透，很多人栽这里
- 穿透有时是特性：多个 case 可共用一段代码
- case 常量必须互不相同；default 可选、位置任意

```c
switch ((int)(score / 10))
{
    case 10:
    case 9:  printf("等级：A\n"); break;   // 10和9共用
    case 8:  printf("等级：B\n"); break;
    case 7:  printf("等级：C\n"); break;
    case 6:  printf("等级：D\n"); break;
    default: printf("等级：E\n"); break;
}
```

---

## 第6章 循环结构

**一、三种循环对比【重点】**

| 循环                            | 特点                                              | 适用场景               |
| ------------------------------- | ------------------------------------------------- | ---------------------- |
| `for(初始化; 条件; 更新) { }` | 最常用，次数明确                                  | 遍历数组、固定次数     |
| `while(条件) { }`             | 先判断后执行，可能一次不执行                      | 条件驱动的循环         |
| `do { } while(条件);`         | 先执行后判断，**至少执行1次**，末尾分号别丢 | 菜单、至少跑一次的场景 |

for 执行顺序：初始化（仅1次）→ 判断 → 真→执行循环体→更新→再判断；假→跳出

**二、循环控制**

- `break`：终止**整个循环**，直接跳出
- `continue`：结束**本次循环**，进入下一次判断

**三、循环嵌套**：外层执行 1 次，内层完整跑一轮（经典练习：九九乘法表）

**四、示例**

```c
int sum = 0;
for (int i = 1; i <= 100; i++)
    sum += i;
printf("1-100的和：%d\n", sum);   // 5050
```

> 嵌入式里的循环：裸机主程序就是一个 `while (1) { }` 死循环，永远不退出 main；等硬件就绪也是循环——`while (!(USART_SR & (1 << 5))) { }` 死等串口标志位。刚学的位运算这就用上了。

---

## 第7章 数组

**一、核心概念**

数组是一组**相同类型、连续存储**的数据，用数组名+下标访问。

**二、定义、初始化与遍历**

```c
int a[10];                 // 定义：长度必须是常量，下标从0开始，最大下标=长度-1

int a[5] = {1,2,3,4,5};    // 全部初始化
int a[5] = {1,2,3};        // 部分初始化，剩余自动补0
int a[]  = {1,2,3,4,5};    // 省略长度，自动推断为5
int a[5] = {0};            // 全部清0（嵌入式常用）

for (int i = 0; i < 5; i++)
    printf("%d ", a[i]);   // 遍历
```

**三、C 数组的三个"反直觉"**

1. **无边界检查**：`a[10]` 越界编译不报错，运行时悄悄踩坏别的内存——嵌入式里可能直接踩到寄存器，灵异 bug 之源
2. **数组名 ≈ 首元素地址**：是地址常量，不可修改、不可自增
3. **没有 len()**：长度自己算 `sizeof(a) / sizeof(a[0])`；注意**数组传进函数后会退化成指针**，sizeof 失效，长度要另传参数

**四、二维数组【了解】**

- 定义：`int a[3][4];`，按**行优先**存储
- 遍历：双层 for，外层遍历行，内层遍历列

---

## 第8章 字符串

**一、核心本质**

C 没有专门的字符串类型，字符串 = **末尾带 `'\0'` 结束符**的字符数组。

```c
char str1[] = "hello";                    // 系统自动加'\0'，实际占6字节
char str2[] = {'h','e','l','l','o','\0'}; // 手动加结束符
```

`'\0'` 是字符串的唯一终点标志：printf、strcpy 等全靠找它，漏了就会输出乱码

**二、输入输出**

用 `%s` 整体输入输出；数组名本身就是地址，scanf 里**不用加 `&`**：`scanf("%s", str1);`

**三、字符串处理函数（需引入 `<string.h>`）**

| 函数                   | 功能             | 必记规则                    |
| ---------------------- | ---------------- | --------------------------- |
| `strlen(str)`        | 求实际长度       | 不含末尾`'\0'`            |
| `strcpy(str1, str2)` | 复制             | 不能用`str1 = str2` 赋值  |
| `strcmp(str1, str2)` | 比较，相等返回 0 | 不能用`str1 == str2` 比较 |
| `strcat(str1, str2)` | 拼接             | str1 空间必须足够           |

**四、安全提醒**：工程里优先用带长度限制的版本 `strncpy`、`strncat`、`snprintf`，防止缓冲区溢出——C 最经典的安全漏洞来源。

---

## 第9章 函数

**一、核心概念**：函数是独立功能的代码块；一个 C 程序 = 一个 main + 若干其他函数。

**二、标准定义**

```c
返回值类型 函数名(形参类型 形参1, ...)
{
    函数体;
    return 返回值;   // 无返回值写 void
}

int max(int a, int b)   // 示例：求最大值
{
    return a > b ? a : b;
}
```

- 铁则：函数**不能嵌套定义**，只能嵌套调用

**三、声明与定义分离【嵌入式工程的地基】**

- 函数定义在调用**之后**时，必须在调用前先声明：`int max(int a, int b);`
- 多文件开发的标准结构：**声明写在 .h 头文件，定义写在 .c 源文件**，谁要用谁 `#include`
- 嵌入式工程全是这个结构：一个外设一对 `.c` + `.h`（led.c/led.h、uart.c/uart.h……），main.c 只管调用

**四、参数传递**

- C 语言是**值传递**：实参把值复制给形参，形参改了不影响实参
- 想让函数改实参 → **传指针**（下一章的核心）

**五、递归【嵌入式慎用】**

- 两个必要条件：① 递归出口；② 递推公式
- MCU 栈空间通常只有几 KB，深递归直接爆栈死机——能改循环就改循环

**六、static 局部变量【嵌入式高频】**

- 只初始化一次，值跨函数调用保留——统计中断触发次数、保存模块运行状态都靠它

---

## 第10章 指针（上）

**一、核心概念【必背】**

- 地址：内存中每个字节的编号
- 指针：本质就是内存地址
- 指针变量：专门存放地址的变量

**二、定义与使用**

```c
int a = 10;
int *p = &a;    // & 取地址：让 p 指向 a
*p = 20;        // * 解引用：访问/修改 p 指向的变量，等价于 a = 20
int *q = NULL;  // 空指针，指向"什么都没有"，避免野指针
```

- 定义格式：`基类型 *指针名;`
- `&` 取地址、`*` 解引用——两个运算符玩明白，指针就入门了

**三、指针与数组**

- 数组名是首元素地址：`int a[5]; int *p = a;`（不用写 `&a`）
- 等价关系：`a[i]` ≡ `*(a+i)` ≡ `*(p+i)` ≡ `p[i]`
- `p++`：指针向后移动**一个元素**（不是 1 个字节！移动字节数 = 基类型大小）

**四、指针与字符串**

```c
char str1[] = "hello";   // 字符数组：内容可修改
char *str2 = "hello";    // 字符指针：指向常量区，内容不可改！
```

- `str1[0] = 'H';` 合法；`str2[0] = 'H';` 直接段错误——经典坑

---

## 第11章 指针（下）

**一、指针作函数参数：突破值传递**

```c
void swap(int *x, int *y)    // 形参接收地址
{
    int t = *x;
    *x = *y;
    *y = t;
}

int a = 1, b = 2;
swap(&a, &b);                // 调用时传地址 → a、b 真的被交换了
```

- 规律：函数想改调用者的变量，就传地址；想"返回"多个值，也靠传指针带出

**二、const 与指针的组合**

- `const char *p;`：指向的内容不可改，常用于只读参数：`void log_print(const char *str);`

**三、函数指针【嵌入式回调的底层原理】**

```c
int add(int a, int b) { return a + b; }

int (*fp)(int, int) = add;   // fp 是指向函数的指针
printf("%d\n", fp(1, 2));    // 通过指针调用 → 3
```

- 标准库 `qsort` 靠传入比较函数指针排序任意类型
- 嵌入式里的**中断回调、事件注册**（`HAL_UART_RxCpltCallback`）就是这个套路

**四、指针 = 读写硬件的钥匙【嵌入式核心认知】**

MCU 的每个寄存器都映射在一个固定物理地址上。**读写寄存器 = 定义一个指向固定地址的指针，然后解引用**：

```c
#include <stdint.h>

// 把地址 0x40020014 定义成"GPIOA输出数据寄存器"
#define GPIOA_ODR   (*(volatile uint32_t *)0x40020014)

GPIOA_ODR |= (1 << 5);   // 第5位置1 → PA5 输出高电平 → LED 亮
```

拆解这个宏：`(uint32_t *)0x40020014` 把整数地址强转成指针 → 前面加 `*` 解引用得到寄存器本身 → `volatile` 禁止编译器优化。配合位运算四操作，硬件就听你指挥了。

---

## 第12章 结构体与自定义类型

**一、结构体 struct【本章核心】**

把不同类型的数据组合成一个整体（≈ 没有方法的 class）。

```c
struct Student              // 定义类型（末尾分号不能丢！）
{
    int id;
    char name[20];
    float score;
};

struct Student stu1 = {1001, "张三", 90.5};   // 定义变量并初始化
stu1.id = 1002;                                // 用 . 访问成员

struct Student *p = &stu1;
p->score = 95.0;                               // 指针用 -> 访问成员

struct Student class1[30];                     // 结构体数组：批量管理
```

**二、typedef：给类型起别名【嵌入式库的标准写法】**

```c
typedef struct {
    int x, y;
} Point;              // 定义和起名一步到位
Point p = {1, 2};
```

**三、嵌入式应用：寄存器映射的原理**

官方库的 `GPIOA->ODR` 不是魔法——就是把一个结构体指针指向外设寄存器组的起始地址：

```c
typedef struct {
    volatile uint32_t MODER;    // 偏移 0x00：模式寄存器
    volatile uint32_t OTYPER;   // 偏移 0x04
    // ...
    volatile uint32_t ODR;      // 输出数据寄存器
} GPIO_TypeDef;

#define GPIOA   ((GPIO_TypeDef *)0x40020000)   // GPIOA 寄存器组起始地址

GPIOA->ODR |= (1 << 5);   // 等价于第12页的宏写法，但更优雅
```

结构体成员按顺序紧密排列在内存里，正好和寄存器的地址偏移一一对应——**这就是全部 HAL 库的底层秘密**。

**四、union 与 enum【嵌入式常用】**

- union：成员共享同一块内存——把 4 字节整数当 4 个单字节拆开，串口发数据、协议解析常用
- enum：把取值一一列举 `enum State { IDLE, RUNNING, PAUSED };`——写**状态机**必备，比魔数可读性好太多

---

## 第13章 预处理与存储类别

**一、宏定义 #define#define**

```c
#define MAX 100                  // 无参宏：纯文本替换，习惯全大写
#define SQUARE(x) ((x) * (x))    // 带参宏：参数和整体都必须加括号！
```

- 括号陷阱：`#define SQUARE(x) x*x` 遇到 `SQUARE(a+b)` 展开成 `a+b*a+b`，结果全错

**二、条件编译【嵌入式天天用】**

1. 头文件防重复包含（**每个 .h 必写**）：

```c
#ifndef __LED_H
#define __LED_H
void LED_Init(void);
#endif
```

2. 调试开关 / 跨型号裁剪：

```c
#define DEBUG 1
#if DEBUG
    printf("temp = %d\n", temp);   // 发布时改成 0，调试信息全部消失
#endif
```

**三、嵌入式四大修饰符【实战/面试高频】**

| 修饰符       | 作用                                                    | 嵌入式典型用途                     |
| ------------ | ------------------------------------------------------- | ---------------------------------- |
| `volatile` | 禁止编译器优化，每次必须从内存重新读                    | 硬件寄存器、中断与主程序共享的变量 |
| `const`    | 只读，定义后不可改                                      | 大常量表放 Flash 省 RAM；只读参数  |
| `static`   | ① 局部变量只初始化一次；② 全局变量/函数只在本文件可见 | 模块私有化，防止命名冲突           |
| `extern`   | 声明变量/函数在别的文件定义                             | 跨文件共享（一般写在 .h 里）       |

volatile 经典坑（一定要懂）：

```c
// flag 在中断服务函数里被置 1，主程序等它
while (flag == 0) { }   // 没加 volatile → 编译器"优化"成只读一次 → 死循环！
// 正确写法：volatile uint8_t flag;
```

---

## 第14章 内存布局速览

| 分区        | 存什么                       | 嵌入式要点                                        |
| ----------- | ---------------------------- | ------------------------------------------------- |
| 栈 stack    | 局部变量、函数调用现场       | 只有几 KB，**爆栈 = 死机**（死机头号原因）  |
| 堆 heap     | `malloc`/`free` 手动管理 | 裸机慎用，易碎片化                                |
| 全局/静态区 | 全局变量、static 变量        | 一直占着 RAM                                      |
| 代码/常量区 | 函数、const 常量、字符串常量 | 放 Flash，不占 RAM——**RAM 比 Flash 金贵** |

```c
int *p = (int *)malloc(10 * sizeof(int));
if (p == NULL) { /* 处理失败 */ }
free(p);  p = NULL;   // 用完必须释放并置空
```

## 第15章 综合实战：寄存器版 GPIO 点灯

```c
#include <stdint.h>

// 寄存器地址映射（以 STM32F4 的 GPIOA 为例）
#define RCC_AHB1ENR   (*(volatile uint32_t *)0x40023830)
#define GPIOA_MODER   (*(volatile uint32_t *)0x40020000)
#define GPIOA_ODR     (*(volatile uint32_t *)0x40020014)

int main(void)
{
    RCC_AHB1ENR |= (1 << 0);        // 1. 使能 GPIOA 时钟（不开时钟外设不工作！）
    GPIOA_MODER &= ~(3 << 10);      // 2. 先清空 PA5 的2个模式位（清位）
    GPIOA_MODER |=  (1 << 10);      // 3. PA5 设为输出模式（置位）

    while (1)                       // 4. 裸机主循环
    {
        GPIOA_ODR ^= (1 << 5);      //    翻转 PA5 → LED 闪烁（翻转）
        for (volatile int i = 0; i < 500000; i++);  // 简陋延时
    }
}
```

**这段代码用到了本笔记的全部核心：定长类型 + 宏定义 + 指针解引用固定地址 + volatile + 位运算四操作 + while(1) 主循环——这就是嵌入式 C 的日常。**

## 附录：避坑速查表【写代码/调试必查】

1. 忘记分号、用了中文分号 → 编译报错
2. 混淆 `=` 和 `==` → 逻辑全错，编译还不一定报
3. `scanf` 忘记加 `&` → 程序崩溃
4. 数组下标越界 → 编译不报，运行时踩内存（嵌入式里可能踩到寄存器）
5. `switch` 忘记 `break` → case 穿透
6. 循环忘记更新变量 → 死循环
7. 字符串漏 `'\0'` → 输出乱码
8. 指针未初始化就用 → 野指针，程序跑飞
9. 函数定义在调用之后却没声明 → 编译报错/警告
10. 结构体定义末尾漏分号 → 编译报错
11. **中断共享变量没加 volatile** → 编译器优化出灵异 bug
12. **局部大数组/深递归** → 栈溢出死机，还查不到原因
13. **部分 MCU 的 printf 默认不支持 %f** → 浮点输出异常，需在工程配置里开启

---

**到这里，15 页过完：前 9 页你会了 C，后 6 页你知道了 C 怎么操纵硬件。下一站：开个最小项目（点灯 → 串口收发 → 定时器中断），在实战里把知识焊死。**
